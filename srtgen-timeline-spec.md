# srtgen タイムライン編集モード 実装仕様書

対象リポジトリ: `Shotaroo24/new-scripts-tool`（main）
対象ファイル: `index.html` / `styles.css` / `app.js`（ローカルファイル間の分割のみ可。CDN・外部ライブラリ・ビルドステップは導入しない。外部依存ゼロ・全処理クライアント完結は絶対条件）
既存仕様書: `srtgen-web-spec.md` — 本書と矛盾する場合は本書が優先。ただし「SRT出力の時刻計算（累積丸め方式）」「SRT形式」「区切り文字による分割ルール」は既存仕様をそのまま踏襲する。

---

## 1. 目的

現在のsrtgenは「原稿貼り付け → 12cps自動計算 → SRT出力」の一方通行に加え、動画編集ソフト風のタイムライン編集モードを持つ。本書はタイムライン編集モードの全仕様（データモデル・UI・永続化）を定義する。

作業フロー全体:

```
原稿貼り付け（既存） → [タイムラインで調整] → タイムライン編集モード → [SRTダウンロード]
                          ↑                    |
                          └── [原稿に戻る]（書き戻し・確認ダイアログなし）
```

原稿が編集されずに往復する限り、タイムラインの手動調整（尺・テキスト・ズーム・ヘッド位置）は失われない。

## 2. データモデル

- 字幕は配列 `subs = [{ text: string, durMs: number, edited: boolean, delim: string }, ...]` で保持する。
- **`durMs` は整数ミリ秒。** 内部の時間計算（累積開始時刻・クリップ境界判定・SRT変換）はすべてこの整数msのみで行い、途中で浮動小数を経由しない。
  - 生成時・cps再計算時: `Math.round((文字数 / cps) * 1000)` で整数ms化。
  - ドラッグによる直接指定: px→msの変換値を`Math.round`で1ms単位に丸めるのみ。**人為的なスナップ（0.1秒刻み等）は入れない。**
  - 最小尺は **300ms**。いかなる操作でもこれを下回れない。
- `edited`: ユーザーが尺・テキストを明示的に変更したクリップは `true`。cpsスライダーによる自動再計算は `edited:false` のクリップにのみ適用される。
- `delim`: 原稿パース時にこのクリップの直後で**除去された**区切り文字（`.` `،` `,` のみ）。「残す」系の文字（`؟` `?` `!` `—`）は本文側に既に存在するため `delim` は空文字にする（二重出力防止）。区切り文字が連続する場合、削除系のみを連結して記録する（例: `...` → `delim="..."`）。末尾クリップ（区切りなしで終わる）は `delim=""`。
- 各字幕の開始時刻は**保存しない**。常に先頭からの `durMs` の累積で導出する（整数の加算のみのため誤差蓄積ゼロ）。
- **不変条件: 字幕間ギャップは常に0。** 尺を変更すると後続の開始時刻は自動的にズレる（リップル）。ギャップや重なりが生じる状態は表現不可能であること。

### SRT出力

`durMs` は既に整数msのため、先頭から単純に加算するだけで累積誤差は原理的に発生しない（従来の「累積丸め方式」で行っていたMath.roundは不要になる）。形式は既存どおり標準SRT（`HH:MM:SS,mmm`）、UTF-8、BOMチェックボックス連動。

### 原稿改変禁止ルールとの整合

既存の「原稿テキスト一切改変禁止」は**自動変換の禁止**である。以下は許可される明示的なユーザー操作であり、この禁止事項の対象外とする:
- 本モードで追加する「ユーザーによる明示的なテキスト編集」（タシュキール除去・数字統一・RLM挿入・Unicode正規化などの自動処理は行わない。入力された文字列をそのまま保持・出力する）
- 「原稿に戻る」時の書き戻し（3.3節参照）。クリップ列から原稿テキストを再構成する処理そのものは、ユーザーが編集モードで行った操作の結果を原稿に反映する目的の変換であり、原稿の自動改変ではない。

## 3. モード遷移

### 3.1 基本フロー
1. **入力モード**（既存画面）: 原稿テキストエリア、cps数値入力、BOMチェックボックス、「タイムラインで調整」ボタン。
2. 「タイムラインで調整」押下 → 3.2節の判定に従い、生成または復元して**編集モード**へ。
3. 編集モードの「原稿に戻る」ボタン → **確認ダイアログなし**で即座に、3.3節の書き戻しを行い入力モードへ戻る。`subs` と履歴・ズーム段・ヘッド位置はメモリ上に保持され続ける（破棄しない）。
4. SRTダウンロードは編集モードから実行（`subs` ベース）。

### 3.2 「タイムラインで調整」押下時の判定
- タイムラインを一度も生成/復元していない場合 → 原稿を新規パースして生成する。
- 既にタイムライン状態を持っている場合:
  - 現在の原稿テキストが、現在の `subs` から3.3節の書き戻しルールで再構成した文字列と**完全一致**（コードポイント単位、正規化なし）→ 保存済みのタイムライン状態（`subs` / `edited` / cps / ズーム段 / ヘッド位置 / 履歴）をそのまま復元する。再パースは行わない。
  - 一致しない（原稿が編集された）→ 確認ダイアログ「原稿が変更されています。タイムラインを再構築すると、手動調整した尺はリセットされます。よろしいですか？」を表示。OKなら原稿を新規パースして生成し直す（`edited` はすべて `false` に戻り、履歴・ヘッド位置は初期化。ズーム段は維持）。キャンセルなら何もしない。

### 3.3 書き戻し（原稿に戻る）
- 「原稿に戻る」押下時、現在のクリップ列から原稿テキストを再構成してtextareaに反映する。**確認ダイアログは表示しない。**
- 再構成ルール: `clips.map(c => c.text + c.delim).join('\n')`（各クリップの本文内の改行はそのまま保持）。
- この書き戻しテキストを再度パースすると、通常は元と同じクリップ分割になる（ラウンドトリップ一致）。
- **既知の制約:** 「残す」系文字（`؟` `?` `!` `—`）で終わるクリップをMで結合した場合、その文字は結合後の本文中（末尾ではなく中間）に残る。書き戻し後にこのテキストを再パースすると、本文中に残ったその文字が区切りとして再度検出され、結合前と同様に分割される。この場合のみラウンドトリップは一致しない。ただし3.2節の判定は「現在の原稿テキストと書き戻しテキストの文字列比較」であり再パース結果の比較ではないため、通常の再入場（原稿を編集していない場合）ではこの制約は問題にならない。

## 4. 編集モードUI

上から順に:

### 4.1 ヘッダー（コントロールバー）
- 再生/停止ボタン（再生中はアイコン切替）
- ↑ボタン（前のクリップへ）/ ↓ボタン（次のクリップへ）
- 現在時刻 / 合計時間 の表示（`M:SS.d` 形式、常時更新）
- 「S ここで切る」「M 結合」「戻す（Undo）」「原稿に戻る」「SRTダウンロード」ボタン
- cpsスライダー（8〜18、生成時の値を初期値。4.4節参照）
- 操作ヒントの1行テキスト（直近の操作結果メッセージ表示と兼用。ズーム段変更時にも一時的に表示）

### 4.2 プレビュー枠
- 再生ヘッド位置に表示中の字幕テキストを大きく表示（`dir="rtl"`、中央揃え、20px程度）。動画の代替となる疑似プレビュー。改行を含むテキストは2行で表示する（`white-space: pre-wrap`）。
- **ダブルクリックでインライン編集**: 改行を含まないテキストは `<input dir="rtl">`、改行を含むテキストは `<textarea dir="rtl" rows="2">` に切り替わる。Enterで確定（textareaでも改行は挿入せずそのまま確定）、Escでキャンセル、blurで確定。再生中にダブルクリックした場合は自動停止してから編集開始。編集中は全キーボードショートカット無効。

### 4.3 タイムライン
- 横スクロール可能なコンテナ。
- **表示ズーム**: X（ズームイン）/ Z（ズームアウト）キーで、px/秒を段階的に変更する。段は `[20, 35, 50, 75, 100, 150, 200, 300, 400]`、初期値は75（従来の80px/secに最も近い段）。**ズームは見た目（px/sec）のみを変え、尺・時刻・SRT出力には一切影響しない。**
  - ズーム時は再生ヘッドの画面上のX座標を保つようスクロール位置を補正する。ヘッドが画面外にある場合はビューポート中央の時刻を基準にする。
- 秒目盛りルーラー。目盛り間隔はズーム段に応じて `[5, 2, 1, 0.5, 0.1]` 秒から、ラベル同士が40px以上離れる最小間隔を自動選択する。**ルーラークリックでその時刻に頭出し**。
- 字幕クリップ: 尺に比例した幅のブロックが密着して並ぶ。クリップ内にアラビア語テキスト（RTL、省略記号付き1行）と尺表示（例 `1.9s`、小数第1位）。再生ヘッドが乗っているクリップは強調表示（枠色・背景色変更）。`edited:true` のクリップは左端に細いアクセントカラーのバーで視覚的に区別する。
- クリップ右端に**リサイズハンドル**（幅10px程度、`cursor: ew-resize`）。ドラッグで尺変更（px→msをその時点のズーム段から算出し1ms単位に丸め、最小300ms。スナップなし）。変更中もリアルタイムで後続がリップルして再描画される。
- **再生ヘッド**: 赤い縦線＋上部に小さなつまみ。再生中は実時間で右へ移動。ヘッドが画面端に近づいたら自動横スクロールで追従。

### 4.4 cpsスライダーと再計算
- cpsスライダーを変更すると、`edited:false` のクリップのみ、自身のテキストの文字数（スペース込み・改行除く・コードポイント単位）から尺(ms)を再計算する（0.1秒丸めではなくMath.roundでms整数化・最小300msクランプ）。`edited:true` のクリップは一切変更しない。
- リップルにより開始時刻は自動で再配置される。
- 入力モードのcps数値入力とは同じ値を共有する（編集モードでの変更は入力モード側にも反映される）。

## 5. 操作仕様

| 操作 | 挙動 |
|---|---|
| スペース | 再生/停止のトグル。末尾到達で自動停止。停止状態で末尾にいる場合、再生開始で先頭から。 |
| ↑ / ↑ボタン | クリップ頭から150msより進んでいれば**現在クリップの頭**へ、頭付近なら**前のクリップの頭**へ（音楽プレイヤーの「戻る」挙動）。 |
| ↓ / ↓ボタン | 次のクリップの頭へ。最終クリップでは何もしない。 |
| S / 「S ここで切る」 | ヘッドが乗っているクリップの尺を `ヘッドms − クリップ開始ms` に変更。**テキストは変更しない**（時間のみのトリム）。結果が300ms未満なら拒否してヒント表示。後続はリップル。 |
| M / 「M 結合」 | ヘッド位置のクリップと右隣を結合。テキストは半角スペース連結、尺は合算、`delim` は右クリップのものを引き継ぐ（左クリップの `delim` は破棄）。最終クリップでは拒否してヒント表示。 |
| Delete / Backspace | ヘッド位置のクリップを削除。後続はリップルで詰まる。残存クリップの `edited` フラグには影響しない。 |
| クリップ右端ドラッグ | 尺変更（リップル、1ms単位）。 |
| クリップ本体クリック / ルーラークリック | その位置に頭出し。 |
| X / Z | ズームイン / ズームアウト（4.3節）。 |
| プレビュー枠ダブルクリック | テキスト編集（4.2参照）。 |
| Ctrl+Z（macOSはCmd+Z） | Undo。 |

### Undo
- 対象: 尺変更（ドラッグは1ドラッグ=1履歴）、S、M、削除、テキスト編集、cps再計算（スライダー操作は1回の確定操作=1履歴）。すべて。
- 実装: 操作前に `subs` のディープコピー（`JSON.stringify` で可）をスタックにpush。上限50件。Redoは不要。
- Undo履歴は永続化（localStorage）の対象外。

### キーボードイベントの注意
- ショートカットはテキスト編集中（input/textareaフォーカス中）は無効化すること。
- スペースのデフォルトスクロールは `preventDefault` すること。
- Ctrl/Cmd+Z（Undo）と単体のZ（ズームアウト）を区別すること。

## 6. 永続化（localStorage自動保存）

- 保存キー: `srtgen:session:v1`。保存単位は直近1セッションのみ（上書き保存）。
- 保存内容: 原稿テキスト・cps・BOM設定・現在のモード（`input`/`edit`）・clips（`text`, `durMs`, `delim`, `edited`）・ズーム段・ヘッド位置(ms)。Undo履歴は含まない。
- スキーマにバージョン番号（`version: 1`）を持たせる。読み込み時にバージョン不一致・JSONパース失敗・想定外の構造（型不正・必須フィールド欠落等）を検出した場合は、例外を投げずに黙って破棄し初期状態から開始する。
- 保存タイミング: 状態変更時に300msデバウンス。ドラッグ中・cpsスライダーのドラッグ中・再生中のヘッド位置更新は保存対象外とし、確定時（pointerup / changeイベント / 再生停止時など）にのみ保存する。
- `localStorage.setItem` が失敗した場合（`QuotaExceededError` 等）は例外を握りつぶし、保存できなくてもアプリの動作は継続する。
- ページ読み込み時に自動復元する。復元した場合のみ、ヘッダ付近に控えめな表示（「前回の続きを復元しました」＋「新規作成」ボタン）を出す。
- 「新規作成」ボタン押下 → 確認ダイアログ → OKでlocalStorageをクリアしページを再読み込みして初期状態に戻す。

## 7. 非スコープ（やらないこと）

- 動画ファイルの読み込み・プレビュー（将来の拡張候補。データモデルが壊れない範囲で拡張点を意識するのは可だが、実装しない）
- 外部ライブラリの導入（React・ドラッグ系ライブラリ等一切不可。Vanilla JS + Pointer Events）
- Redo、複数選択、クリップの並べ替え（順序は原稿順で固定）
- フォント・色などの見た目調整機能（Filmora側でやる前提）
- テキスト編集時の尺の自動再計算（尺は据え置き。変えたければ手動ドラッグ）
- fpsスナップ（Filmora実機で1フレームの隙間が出た場合に別途検討）
- JSONエクスポート／インポート（localStorageによるセッション自動保存とは別。手動でのファイル入出力は行わない）

## 8. テスト

`tests/logic.test.js` に実装（既存テストは全てms/delim前提に更新のうえ維持・全PASSが条件）:

1. リップル: `[1000, 2000, 1500]`(ms) の2番目を3000msに変更 → 開始時刻が `[0, 1000, 4000]` になる
2. トリム: 開始1000ms・尺2000msのクリップに対しヘッド2400msでS → 尺1400ms
3. トリム下限: 結果が300ms未満になるSは拒否され状態不変
4. 結合: テキストがスペース連結、尺が合算、配列長が1減る、`delim`は右クリップのものを引き継ぐ
5. 削除: 配列長が1減り、後続の開始時刻が繰り上がる、残存クリップの`edited`は不変
6. SRT出力: 任意のms列で累積が整数演算のみで完全一致し、前の字幕の終了 = 次の字幕の開始であること
7. Undo: 任意の操作後にUndoで直前の状態へ完全復元
8. **ms精度**: 多数クリップ（100枚超）の累積でも整数演算のみのため誤差ゼロであること
9. **ズーム無影響**: 全ズーム段で `subsToCues` / `buildSrt` の出力が完全に同一であること
10. **ラウンドトリップ**: 通常ケースで書き戻し→再パースが元のtext/delimと一致すること。既知の例外（残す文字終わりのクリップをM結合した場合）は再パース後にクリップ数が変わることを明示的に確認すること
11. **edited尺の維持**: 原稿未編集判定（textEquals）がtrueの場合、再パースを経ずにeditedクリップの手動調整尺が保持されること
12. **永続化**: 有効なセッションのserialize/deserializeが往復できること。JSONパース失敗・バージョン不一致・型不正の場合にdeserializeがnullを返す（例外を投げない）こと

ロジック（clipAt、リップル計算、トリム、結合、削除、SRT生成、書き戻し、ズーム変換、セッション検証）はUIから分離した純粋関数として実装し、Nodeでテスト可能にすること。

## 9. 受け入れ基準

- [ ] 既存の入力モードの機能・見た目・受け入れテストが一切壊れていない
- [ ] 原稿貼り付け → 生成 → タイムラインで尺調整・トリム・結合・削除・テキスト編集 → SRTダウンロード が一連で動作する
- [ ] 出力SRTに0.001秒の隙間もない（全区間でギャップ0）、ms精度の内部表現に浮動小数が混入しない
- [ ] X/Zでのズームが尺・SRT出力に一切影響しない
- [ ] 「原稿に戻る」に確認ダイアログがなく、書き戻し後に未編集のまま再入場すると手動調整が完全に復元される
- [ ] 原稿を編集してから再入場すると確認ダイアログが出て、OK時のみ再構築される
- [ ] ページを再読み込みしても直近のセッションが復元され、破損データでもアプリが停止しない
- [ ] 上記8節のテストが全PASS
- [ ] `index.html` / `styles.css` / `app.js` のローカルファイル分割のみ、外部依存ゼロを維持
- [ ] Playwrightでの実ブラウザ動作確認（再生・S・M・ドラッグ・編集・Undo・ズーム・書き戻し・永続化・ダウンロード）
