# srtgen タイムライン編集モード 実装仕様書

対象リポジトリ: `Shotaroo24/new-scripts-tool`（main）
対象ファイル: `index.html` / `styles.css` / `app.js`（ローカルファイル間の分割のみ可。CDN・外部ライブラリ・ビルドステップは導入しない。外部依存ゼロ・全処理クライアント完結は絶対条件）
既存仕様書: `srtgen-web-spec.md` — 本書と矛盾する場合は本書が優先。ただし「SRT出力の時刻計算（累積丸め方式）」「SRT形式」「区切り文字による分割ルール」は既存仕様をそのまま踏襲する。

---

## 1. 目的

現在のsrtgenは「原稿貼り付け → 12cps自動計算 → SRT出力」の一方通行。これに**タイムライン編集モード**を追加し、生成後の字幕の尺・テキストを動画編集ソフト風のUIで直感的に調整してからSRT出力できるようにする。

作業フロー全体:

```
原稿貼り付け（既存） → [生成ボタン] → タイムライン編集モード → [SRTダウンロード]
                          ↑                    |
                          └── [原稿に戻る]（確認ダイアログ → 編集内容破棄）
```

## 2. データモデル

- 字幕は配列 `subs = [{ text: string, dur: number }, ...]` で保持する。
- `dur` は秒（小数第1位まで、0.1刻み）。生成時の初期値は既存の読み速度計算（cps、デフォルト12、スペース込み・改行除く・コードポイント単位 `Array.from`）。
- 各字幕の開始時刻は**保存しない**。常に先頭からの `dur` の累積で導出する。
- **不変条件: 字幕間ギャップは常に0。** 尺を変更すると後続の開始時刻は自動的にズレる（リップル）。ギャップや重なりが生じる状態は表現不可能であること。
- 最小尺は **0.3秒**。いかなる操作でもこれを下回れない。

### SRT出力

既存の累積丸め方式を必ず流用する: 有効尺を先頭から累積し、累積秒×1000を `Math.round` してミリ秒境界を決定（誤差蓄積ゼロ）。形式は既存どおり標準SRT（`HH:MM:SS,mmm`）、UTF-8、BOMチェックボックス連動。

### 原稿改変禁止ルールとの整合

既存の「原稿テキスト一切改変禁止」は**自動変換の禁止**である。本モードで追加する「ユーザーによる明示的なテキスト編集」は許可される。ただし編集されたテキストに対しても、タシュキール除去・数字統一・RLM挿入・Unicode正規化などの自動処理は一切行わない。入力された文字列をそのまま保持・出力する。

## 3. モード遷移

1. **入力モード**（既存画面）: 原稿テキストエリア、cpsスライダー、BOMチェックボックス。「タイムラインで調整」ボタンを追加。
2. 「タイムラインで調整」押下 → 既存の分割ロジックで `subs` を生成し、**編集モード**へ。原稿テキストエリアとcpsは編集不可（非表示または無効化）。cpsは生成時の初期尺計算にのみ使う。
3. 編集モードの「原稿に戻る」ボタン → 確認ダイアログ「タイムラインでの編集内容は破棄されます。よろしいですか？」→ OKで入力モードへ戻り、`subs` と履歴を破棄。
4. SRTダウンロードは編集モードから実行（既存のダウンロード処理を `subs` ベースに切り替え）。

## 4. 編集モードUI

上から順に:

### 4.1 ヘッダー（コントロールバー）
- 再生/停止ボタン（再生中はアイコン切替）
- ↑ボタン（前のクリップへ）/ ↓ボタン（次のクリップへ）
- 現在時刻 / 合計時間 の表示（`M:SS.d` 形式、常時更新）
- 「S ここで切る」「M 結合」「戻す（Undo）」「原稿に戻る」「SRTダウンロード」ボタン
- 操作ヒントの1行テキスト（直近の操作結果メッセージ表示と兼用）

### 4.2 プレビュー枠
- 再生ヘッド位置に表示中の字幕テキストを大きく表示（`dir="rtl"`、中央揃え、20px程度）。動画の代替となる疑似プレビュー。
- **ダブルクリックでインライン編集**: `<input dir="rtl">` に切り替わり、Enterで確定、Escでキャンセル、blurで確定。再生中にダブルクリックした場合は自動停止してから編集開始。編集中は全キーボードショートカット無効。

### 4.3 タイムライン
- 横スクロール可能なコンテナ。スケールは 1秒 = 80px 基準（定数化し、Ctrl+ホイールでのズームは任意実装・優先度低）。
- 秒目盛りルーラー（1秒刻み、ラベル付き）。**ルーラークリックでその時刻に頭出し**。
- 字幕クリップ: 尺に比例した幅のブロックが密着して並ぶ。クリップ内にアラビア語テキスト（RTL、省略記号付き1行）と尺表示（例 `1.9s`）。再生ヘッドが乗っているクリップは強調表示（枠色・背景色変更）。
- クリップ右端に**リサイズハンドル**（幅10px程度、`cursor: ew-resize`）。ドラッグで尺変更（0.1秒刻みに丸め、最小0.3秒）。変更中もリアルタイムで後続がリップルして再描画される。
- **再生ヘッド**: 赤い縦線＋上部に小さなつまみ。再生中は実時間で右へ移動。ヘッドが画面端に近づいたら自動横スクロールで追従。

## 5. 操作仕様

| 操作 | 挙動 |
|---|---|
| スペース | 再生/停止のトグル。末尾到達で自動停止。停止状態で末尾にいる場合、再生開始で先頭から。 |
| ↑ / ↑ボタン | クリップ頭から0.15秒より進んでいれば**現在クリップの頭**へ、頭付近なら**前のクリップの頭**へ（音楽プレイヤーの「戻る」挙動）。 |
| ↓ / ↓ボタン | 次のクリップの頭へ。最終クリップでは何もしない。 |
| S / 「S ここで切る」 | ヘッドが乗っているクリップの尺を `ヘッド時刻 − クリップ開始時刻`（0.1秒丸め）に変更。**テキストは変更しない**（時間のみのトリム）。結果が0.3秒未満なら拒否してヒント表示。後続はリップル。 |
| M / 「M 結合」 | ヘッド位置のクリップと右隣を結合。テキストは半角スペース連結、尺は合算。最終クリップでは拒否してヒント表示。 |
| Delete / Backspace | ヘッド位置のクリップを削除。後続はリップルで詰まる。 |
| クリップ右端ドラッグ | 尺変更（リップル）。 |
| プレビュー枠ダブルクリック | テキスト編集（4.2参照）。 |
| Ctrl+Z（macOSはCmd+Z） | Undo。 |

### Undo
- 対象: 尺変更（ドラッグは1ドラッグ=1履歴）、S、M、削除、テキスト編集。すべて。
- 実装: 操作前に `subs` のディープコピー（`JSON.stringify` で可）をスタックにpush。上限50件。Redoは不要。

### キーボードイベントの注意
- ショートカットはテキスト編集中（input/textareaフォーカス中）は無効化すること。
- スペースのデフォルトスクロールは `preventDefault` すること。

## 6. 非スコープ（やらないこと）

- 動画ファイルの読み込み・プレビュー（将来の拡張候補。データモデルが壊れない範囲で拡張点を意識するのは可だが、実装しない）
- 外部ライブラリの導入（React・ドラッグ系ライブラリ等一切不可。Vanilla JS + Pointer Events）
- Redo、複数選択、クリップの並べ替え（順序は原稿順で固定）
- フォント・色などの見た目調整機能（Filmora側でやる前提）
- テキスト編集時の尺の自動再計算（尺は据え置き。変えたければ手動ドラッグ）

## 7. テスト

`tests/logic.test.js` に追加（既存テストは全て維持・全PASSが条件）:

1. リップル: `[1.0, 2.0, 1.5]` の2番目を3.0に変更 → 開始時刻が `[0, 1.0, 4.0]` になる
2. トリム: 開始1.0秒・尺2.0秒のクリップに対しヘッド2.4秒でS → 尺1.4秒
3. トリム下限: 結果が0.3未満になるSは拒否され状態不変
4. 結合: テキストがスペース連結、尺が合算、配列長が1減る
5. 削除: 配列長が1減り、後続の開始時刻が繰り上がる
6. SRT出力の累積丸め: 編集後の任意の尺列（例 `[0.7, 1.3, 2.1]`）で、累積秒×1000のMath.roundと出力タイムコードが完全一致し、前の字幕の終了 = 次の字幕の開始であること
7. Undo: 任意の操作後にUndoで直前の状態へ完全復元

ロジック（clipAt、リップル計算、トリム、結合、削除、SRT生成）はUIから分離した純粋関数として実装し、Nodeでテスト可能にすること。

## 8. 受け入れ基準

- [ ] 既存の入力モードの機能・見た目・受け入れテストが一切壊れていない
- [ ] 原稿貼り付け → 生成 → タイムラインで尺調整・トリム・結合・削除・テキスト編集 → SRTダウンロード が一連で動作する
- [ ] 出力SRTに0.001秒の隙間もない（全区間でギャップ0）
- [ ] 上記7のテストが全PASS
- [ ] `index.html` / `styles.css` / `app.js` のローカルファイル分割のみ、外部依存ゼロを維持
- [ ] Playwrightでの実ブラウザ動作確認（再生・S・M・ドラッグ・編集・Undo・ダウンロード）
